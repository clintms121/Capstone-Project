# docs

Drop the compiled capstone paper here as `research_paper.pdf`. The
"Download Research Paper" button on the Research screen
(`graphics/research_screen.cpp`) opens `docs/research_paper.pdf` in the
system's default PDF viewer; until that file exists, the button reports
that it's missing instead of failing silently.
