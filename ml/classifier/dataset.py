"""Spectrogram dataset: Gravity Spy glitch classes + a "real signal" class.

Stub. Split by event/glitch id, never within an event, so no near-duplicate
spectrograms leak across train/val/test.
"""

def load_dataset(spectrogram_dir: str):
    raise NotImplementedError
